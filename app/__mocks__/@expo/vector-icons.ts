import React from 'react';
import { Text } from 'react-native';

export const Ionicons = (props: any) => React.createElement(Text, props, 'Icon');
export const MaterialIcons = (props: any) => React.createElement(Text, props, 'Icon');

export default {
  Ionicons,
  MaterialIcons,
};
